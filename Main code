#中文标注下，是python代码
#还需要强调，这个代码没有运行过

import random

def generate_even_number(): return random.randrange(100, 1001, 2)

if name == "main": even_number = generate_even_number() print("Random even number between 100 and 1000:", even_number)
output=name
